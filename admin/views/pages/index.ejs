<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Seiten SEO | Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-slate-50">
    <div class="min-h-screen flex">
        <!-- Sidebar -->
        <aside class="bg-white border-r border-slate-200 w-64 flex-shrink-0 fixed h-full z-20">
            <div class="p-6 border-b border-slate-100">
                <h1 class="text-xl font-bold text-slate-900">SchauAlles</h1>
            </div>
            <nav class="p-4 space-y-1">
                <a href="/" class="flex items-center gap-3 px-4 py-3 rounded-xl text-slate-600 hover:bg-slate-50 hover:text-slate-900 font-medium transition-colors">Dashboard</a>
                <a href="/blog" class="flex items-center gap-3 px-4 py-3 rounded-xl text-slate-600 hover:bg-slate-50 hover:text-slate-900 font-medium transition-colors">Blog Beiträge</a>
                <a href="/guides" class="flex items-center gap-3 px-4 py-3 rounded-xl text-slate-600 hover:bg-slate-50 hover:text-slate-900 font-medium transition-colors">Hilfe Guides</a>
                <a href="/pages" class="flex items-center gap-3 px-4 py-3 rounded-xl bg-blue-50 text-blue-700 font-medium transition-colors">Seiten SEO</a>
            </nav>
        </aside>

        <!-- Main Content -->
        <div class="flex-1 ml-64 p-8 lg:p-12">
            <header class="mb-10 flex justify-between items-center">
                <div>
                    <h2 class="text-3xl font-bold text-slate-900">Seiten SEO</h2>
                    <p class="text-slate-500 mt-2">Verwalten Sie Meta-Titel und Beschreibungen für Ihre Seiten.</p>
                </div>
            </header>

            <div class="bg-white rounded-2xl shadow-sm border border-slate-200 overflow-hidden">
                <table class="w-full text-left">
                    <thead>
                        <tr class="bg-slate-50 border-b border-slate-100">
                            <th class="px-6 py-4 font-semibold text-slate-900">Seite / Route</th>
                            <th class="px-6 py-4 font-semibold text-slate-900">Titel</th>
                            <th class="px-6 py-4 font-semibold text-slate-900">Beschreibung</th>
                            <th class="px-6 py-4 font-semibold text-slate-900 text-right">Aktionen</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-100">
                        <% pages.forEach(page => { %>
                        <tr class="hover:bg-slate-50 transition-colors">
                            <td class="px-6 py-4">
                                <div class="font-medium text-slate-900"><%= page.page_identifier %></div>
                                <div class="text-sm text-slate-500"><%= page.route %></div>
                            </td>
                            <td class="px-6 py-4 text-slate-600 max-w-xs truncate" title="<%= page.title %>">
                                <%= page.title %>
                            </td>
                            <td class="px-6 py-4 text-slate-600 max-w-xs truncate" title="<%= page.description %>">
                                <%= page.description %>
                            </td>
                            <td class="px-6 py-4 text-right">
                                <a href="/pages/edit/<%= page.id %>" class="text-brand-600 hover:text-brand-700 font-medium text-blue-600 hover:underline">Bearbeiten</a>
                            </td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>

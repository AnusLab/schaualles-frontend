<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Einstellungen | SchauAlles Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-gradient-to-br from-slate-50 to-slate-100 min-h-screen">
    <div class="flex h-screen overflow-hidden">
        <%- include('partials/sidebar', { currentPage: 'settings' }) %>
        
        <div class="flex-1 lg:ml-72 overflow-auto">
            <%- include('partials/topbar') %>
            
            <main class="p-6 lg:p-8">
                <!-- Header -->
                <div class="mb-8">
                    <h1 class="text-3xl font-bold text-slate-900 mb-2">Einstellungen</h1>
                    <p class="text-slate-600">Konfigurieren Sie Ihre Admin-Panel Einstellungen</p>
                </div>

                <form action="/settings/save" method="POST" class="max-w-3xl">
                    <!-- AI Settings Card -->
                    <div class="bg-white rounded-2xl shadow-sm border border-slate-200 overflow-hidden mb-6">
                        <div class="bg-gradient-to-r from-blue-600 to-blue-700 px-6 py-4">
                            <div class="flex items-center gap-3">
                                <div class="w-10 h-10 bg-white/20 backdrop-blur-sm rounded-xl flex items-center justify-center">
                                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path></svg>
                                </div>
                                <div>
                                    <h2 class="text-lg font-bold text-white">KI-Einstellungen</h2>
                                    <p class="text-sm text-blue-100">OpenRouter API für Content-Generierung</p>
                                </div>
                            </div>
                        </div>
                        
                        <div class="p-6 space-y-6">
                            <!-- API Key -->
                            <div>
                                <label for="ai_api_key" class="block text-sm font-semibold text-slate-900 mb-2">
                                    API Key
                                    <span class="text-red-500">*</span>
                                </label>
                                <input 
                                    type="text" 
                                    id="ai_api_key" 
                                    name="ai_api_key" 
                                    value="<%= settings.ai_api_key || '' %>"
                                    placeholder="sk-or-v1-..."
                                    class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 outline-none transition-all font-mono text-sm"
                                    required
                                >
                                <p class="mt-2 text-xs text-slate-500">Ihr OpenRouter API-Schlüssel für KI-gestützte Content-Generierung</p>
                            </div>

                            <!-- AI Model -->
                            <div>
                                <label for="ai_model" class="block text-sm font-semibold text-slate-900 mb-2">
                                    KI-Modell
                                    <span class="text-red-500">*</span>
                                </label>
                                <select 
                                    id="ai_model" 
                                    name="ai_model"
                                    class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 outline-none transition-all"
                                    required
                                >
                                    <option value="x-ai/grok-beta" <%= settings.ai_model === 'x-ai/grok-beta' ? 'selected' : '' %>>Grok Beta (X.AI)</option>
                                    <option value="anthropic/claude-3.5-sonnet" <%= settings.ai_model === 'anthropic/claude-3.5-sonnet' ? 'selected' : '' %>>Claude 3.5 Sonnet</option>
                                    <option value="openai/gpt-4-turbo" <%= settings.ai_model === 'openai/gpt-4-turbo' ? 'selected' : '' %>>GPT-4 Turbo</option>
                                    <option value="google/gemini-pro-1.5" <%= settings.ai_model === 'google/gemini-pro-1.5' ? 'selected' : '' %>>Gemini Pro 1.5</option>
                                    <option value="meta-llama/llama-3.1-70b-instruct" <%= settings.ai_model === 'meta-llama/llama-3.1-70b-instruct' ? 'selected' : '' %>>Llama 3.1 70B</option>
                                </select>
                                <p class="mt-2 text-xs text-slate-500">Wählen Sie das KI-Modell für SEO-Generierung und Content-Verbesserung</p>
                            </div>

                            <!-- Info Box -->
                            <div class="bg-blue-50 border border-blue-200 rounded-xl p-4">
                                <div class="flex gap-3">
                                    <svg class="w-5 h-5 text-blue-600 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                                    <div class="text-sm text-blue-700">
                                        <p class="font-semibold mb-1">Wie funktioniert die KI-Integration?</p>
                                        <p>Die KI wird verwendet, um automatisch SEO-optimierte Titel, Slugs und Meta-Beschreibungen zu generieren sowie Inhalte zu verbessern.</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Action Buttons -->
                    <div class="flex items-center gap-4">
                        <button 
                            type="submit"
                            class="px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-xl transition-all shadow-lg shadow-blue-600/30 hover:shadow-blue-600/50 hover:scale-105"
                        >
                            <span class="flex items-center gap-2">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                                Einstellungen speichern
                            </span>
                        </button>
                        <a 
                            href="/"
                            class="px-6 py-3 bg-slate-100 hover:bg-slate-200 text-slate-700 font-semibold rounded-xl transition-all"
                        >
                            Abbrechen
                        </a>
                    </div>

                    <!-- Additional Info -->
                    <div class="mt-8 bg-gradient-to-br from-amber-50 to-orange-50 border border-amber-200 rounded-xl p-6">
                        <div class="flex gap-4">
                            <div class="flex-shrink-0">
                                <svg class="w-6 h-6 text-amber-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path></svg>
                            </div>
                            <div>
                                <h3 class="text-sm font-semibold text-amber-900 mb-1">Wichtiger Hinweis</h3>
                                <p class="text-sm text-amber-700">Bewahren Sie Ihren API-Schlüssel sicher auf. Teilen Sie ihn niemals öffentlich. Die Nutzung der OpenRouter API kann Kosten verursachen.</p>
                            </div>
                        </div>
                    </div>
                </form>
            </main>
        </div>
    </div>

    <script>
        const sidebarToggle = document.getElementById('sidebar-toggle');
        const sidebar = document.getElementById('sidebar');
        
        if (sidebarToggle && sidebar) {
            sidebarToggle.addEventListener('click', () => {
                sidebar.classList.toggle('-translate-x-full');
            });
        }
    </script>
</body>
</html>

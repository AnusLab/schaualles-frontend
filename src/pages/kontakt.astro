---
import Layout from '../layouts/Layout.astro';
import { MessageCircle, Mail, Clock, CheckCircle2 } from 'lucide-react';

import { getSeo } from '../lib/seo';

const seo = await getSeo('contact') || {};
const pageTitle = seo.title || "Kontakt & Support | SchauAlles.com";
const pageDescription = seo.description;
---

<Layout title={pageTitle} description={pageDescription}>
  <div class="min-h-screen bg-slate-50 pt-32 pb-24 relative overflow-hidden">
    <!-- Background elements -->
    <div class="absolute inset-0 bg-gradient-to-br from-brand-50 via-white to-slate-100"></div>
    <div class="absolute top-0 right-0 w-[800px] h-[800px] bg-brand-100/30 rounded-full mix-blend-multiply filter blur-3xl opacity-70"></div>
    <div class="absolute bottom-0 left-0 w-[600px] h-[600px] bg-blue-100/30 rounded-full mix-blend-multiply filter blur-3xl opacity-70"></div>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
      <div class="text-center max-w-3xl mx-auto mb-16">
        <h1 class="text-4xl md:text-5xl font-bold text-slate-900 mb-6">
          Wir sind für Sie da
        </h1>
        <p class="text-xl text-slate-600 leading-relaxed">
          Haben Sie Fragen oder benötigen Sie Hilfe bei der Einrichtung? Unser Expertenteam steht Ihnen zur Seite.
        </p>
      </div>

      <!-- Live Chat Card (Centered) -->
      <div class="max-w-2xl mx-auto mb-24">
        <div class="bg-white p-8 md:p-12 rounded-3xl shadow-2xl shadow-brand-900/10 border border-brand-100 relative overflow-hidden group hover:border-brand-200 transition-all duration-300">
          <div class="absolute top-0 right-0 p-8 opacity-5 group-hover:opacity-10 transition-opacity">
            <MessageCircle className="w-48 h-48 text-brand-600" />
          </div>
          
          <div class="relative z-10 text-center">
            <div class="w-20 h-20 bg-brand-100 rounded-2xl flex items-center justify-center text-brand-600 mb-8 mx-auto">
              <MessageCircle className="w-10 h-10" />
            </div>
            
            <h2 class="text-3xl font-bold text-slate-900 mb-4">Live Chat Support</h2>
            <p class="text-lg text-slate-600 mb-8 max-w-lg mx-auto">
              Der schnellste Weg zu uns. Sprechen Sie direkt mit einem unserer deutschen Experten und erhalten Sie sofortige Hilfe.
            </p>

            <div class="flex flex-col sm:flex-row items-center justify-center gap-6 mb-10 text-slate-600">
              <div class="flex items-center gap-2 bg-slate-50 px-4 py-2 rounded-full border border-slate-100">
                <Clock className="w-5 h-5 text-brand-500" />
                <span class="font-medium">Antwortet in Minuten</span>
              </div>
              <div class="flex items-center gap-2 bg-slate-50 px-4 py-2 rounded-full border border-slate-100">
                <CheckCircle2 className="w-5 h-5 text-green-500" />
                <span class="font-medium">100% Deutscher Support</span>
              </div>
            </div>

            <a href="#chatraChatExpanded" class="inline-flex items-center justify-center w-full sm:w-auto px-12 py-4 rounded-xl bg-brand-600 text-white font-bold text-xl hover:bg-brand-700 transition-all transform hover:scale-105 shadow-lg shadow-brand-500/30 gap-3">
              <span class="relative flex h-3 w-3">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-3 w-3 bg-green-500"></span>
              </span>
              Live Chat starten
            </a>
          </div>
        </div>
      </div>

      <!-- Support Benefits -->
      <div class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
        <div class="bg-white p-8 rounded-2xl border border-slate-100 shadow-sm">
          <div class="w-12 h-12 bg-blue-50 rounded-xl flex items-center justify-center text-blue-600 mb-4">
            <CheckCircle2 className="w-6 h-6" />
          </div>
          <h3 class="text-xl font-bold text-slate-900 mb-2">Experten Wissen</h3>
          <p class="text-slate-600">
            Keine Bots, keine Standardantworten. Unsere Mitarbeiter sind technisch geschult und lösen jedes Problem.
          </p>
        </div>

        <div class="bg-white p-8 rounded-2xl border border-slate-100 shadow-sm">
          <div class="w-12 h-12 bg-purple-50 rounded-xl flex items-center justify-center text-purple-600 mb-4">
            <Clock className="w-6 h-6" />
          </div>
          <h3 class="text-xl font-bold text-slate-900 mb-2">Schnelle Lösungen</h3>
          <p class="text-slate-600">
            Wir verstehen, dass Sie nicht warten wollen. Unser Ziel ist es, Ihre Anliegen sofort im Chat zu klären.
          </p>
        </div>

        <div class="bg-white p-8 rounded-2xl border border-slate-100 shadow-sm">
          <div class="w-12 h-12 bg-green-50 rounded-xl flex items-center justify-center text-green-600 mb-4">
            <MessageCircle className="w-6 h-6" />
          </div>
          <h3 class="text-xl font-bold text-slate-900 mb-2">Persönlich & Nah</h3>
          <p class="text-slate-600">
            Wir sprechen Ihre Sprache. Freundlicher, deutscher Kundenservice auf Augenhöhe ist unser Standard.
          </p>
        </div>
      </div>

      <div class="mt-16 text-center">
        <p class="text-slate-500">
          Häufige Fragen beantworten wir auch in unserem <a href="/#faq" class="text-brand-600 font-bold hover:underline">FAQ-Bereich</a>.
        </p>
      </div>
    </div>
  </div>
</Layout>
